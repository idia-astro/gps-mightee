# https://dont-be-afraid-to-commit.readthedocs.io/en/latest/git/commandlinegit.html#some-basic-git-operations # Explaination of some of following command can be found in this website.

# Cloning the gps-mightee repository
$ cd /idia/users/wanga
$ git clone https://github.com/idia-astro/gps-mightee.git # clone gps-mightee to local, get a direcoty of gps-mightee 

# Creating a custom branch and pushing to github
$ cd gps-mightee 
$ git branch -a # show all the branches
$ git checkout -b Wanga_branch master # create a new branch Wanga_branch
$ git checkout -d Wanga_branch # deleting Wanga_branch
$ git push -u origin Wanga_branch # push the new created branch into github
$ git init # To initialize in the custom branch 

# Switching to a different branch
$ git checkout master # go back to master branch
$ git checkout Wanga_branch # go back to Wanga_branch

# Uploading files
# First copy the file into /idia/users/wanga/gps-mightee
$ git checkout Wanga_branch # go to branch you want to add files to
$ git add file_name # add the file
$ git status # check if you need to commit the file
$ git commit -m file_name # commit the file
$ git push -u origin Wanga_branch # push the file to Wanga_branch on github
$ git push origin Wanga_branch --force # if errors arise

